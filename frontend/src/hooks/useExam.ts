/**
 * useExam Hook
 * 
 * Re-export of the useExam hook from ExamContext for convenience.
 * This allows importing from a consistent location.
 */

export { useExam } from '@/src/context/ExamContext';
